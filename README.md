# Demo Page

Source html codes for the demo page demonstration are stored under this directory.

In this demo page, we will compare 4 different ai cover song generation systems: FlexCover (Ours), Suno-v5.5, ACE-Step 1.5 and SongEcho, and highlight FlexCover's superior performance over baselines.

This demo page should contain several sections listed below:
1. Intro. 
    In this section, we should briefly introduce FlexCover's core features, along with a demonstration on a single song.
    Features supported by our model:
        - Full Song Cover (Reference Music + Original Lyrics + New Style / Reference Music + New Lyrics + New Style)
        - Partial-Reference Song Cover (Partial Reference Music + Lyrics for Full Song + New Style)
            Three partial-reference configurations: continuation, inpainting, outpainting
            Continuation: The beginning part (from the start to the beginning of the first chorus) is visible to the model. The model generated the full song with the observed part coherent to the reference and flexible recreation of the remaining part.
            Outpainting: The first chorus is visible to the model, and the model generated the full song with the observed part coherent to the reference and flexible recreation of the remaining part.
            Inpainting: Everything except the first chorus is visible to the model, and the model generated the full song with the observed part coherent to the reference and flexible recreation of the remaining part.
    In this intro section, do not include any other systems. The purpose of this part is to let the audience have a general grasp of what FlexCover can do.

2. Full Song Cover Task
    In this section, we should demonstrate FlexCover's full song cover task against several baselines.
    We should include 8 cases.
    For the first 4 cases, we use reference lyrics, its original lyrics and new style.
    For the last 4 cases, we use reference lyrics, new lyrics and new style.
    For each case, we should present comparison among samples generated by FlexCover, Suno-v5.5, ACE-Step 1.5 and SongEcho.
    The layout for each case from left to right:
    An audio panel playing the reference music, a text panel showing the reference lyrics (collapsable and scrollable to avoid being horizontally overlengthy), a text panel showing the style tag prompts (no need to be collapsable nor scrollable here since style description is usually short), and finally 4 audio panels showing the samples generated by the 4 systems on the right.

3. Partial-Reference Song Cover Task
    In this section, we should demonstrate FlexCover's partial-reference song cover task against several baselines.
    We should include 6 cases, with each configuration (continuation, outpainting, inpainting) having 2 cases.
    For each case, we should present comparison among samples generated by FlexCover, Suno-v5.5, ACE-Step 1.5 and SongEcho.
    The layout for each case from left to right:
    An audio panel playing the full reference music along with an titled additional panel showing the partial visible part and its configuration (continuation/oupainting/inpainting), a text panel showing the full lyrics (collapsable and scrollable to avoid being horizontally overlengthy), a text panel showing the style tag prompts (no need to be collapsable nor scrollable here since style description is usually short), and finally 3 audio panels showing the samples generated by the 3 systems on the right (Suno-v5.5 excluded for this setting).

We should have a table of contents located on the topright of the overall page and allow users to navigate directly to their interested part.

Among this page, samples generated by our system (FlexCover) should always be highlighted to make it visually distinguishable.

Please include placeholders for each lyrics and audio files for now. I will manually update them later.

# Misc

Some additional information that might be helpful to fill in some necessary fields in this demo page:

Paper Title: FlexCover: Flexible Cover Song Generation via Symbolic Lead Sheet Control

Submission Number: 1346

Abstract:
A cover song re-renders an existing piece by preserving its tonal content—melody and chord progression—while reshaping other musical attributes. This suggests a natural formulation for generative cover synthesis with pretrained music language models: explicitly control tonal content, while specifying lyrics and style through text. Existing approaches condition on frame-level features, tightly coupling generation to the reference’s timing and structure, and limiting flexibility such as tempo variation, structural rearrangement, or partial conditioning. To this end, we present FlexCover, a cover generation model that conditions a pretrained text-to-song foundation model on a symbolic lead sheet. Our design enables alignment-free control: generated outputs preserve the tonal signature of the source without frame-level alignment, allowing flexible timing, structure, and segment-level generation. We further introduce a training curriculum with partially mismatched audio–symbolic pairs, improving diversity and robustness. We evaluate FlexCover with objective metrics, standard subjective ratings, and in-depth expert interviews that provide fine-grained diagnostic insights. Experiments show that FlexCover achieves state-of-the-art performance among open-source systems and is competitive with leading commercial models such as Suno-v5.5.

You are not encouraged to directly copy the abstract to the demo page. All long paragraphs in the demo page should be used sparingly.
